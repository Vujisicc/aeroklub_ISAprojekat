spring.datasource.url=jdbc:mysql://localhost:3306/aeroklub?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# JWT (secret must be >= 32 chars; move to an env var in real deployments)
jwt.secret=change-me-aeroklub-super-secret-key-min-32-chars-long-2026
jwt.access-exp-ms=900000
jwt.refresh-exp-ms=604800000

# Seeded admin account (username: admin)
app.admin.password=admin123